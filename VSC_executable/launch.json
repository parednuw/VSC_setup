{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "lldb Debugger Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "cwd": "${fileDirname}",
        }
    ]
}